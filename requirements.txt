# StudySnap Backend Dependencies
# FastAPI 웹 프레임워크
fastapi==0.109.0
uvicorn[standard]==0.27.0

# PDF 처리
PyMuPDF>=1.24.0
pdf2image>=1.17.0
Pillow>=10.0.0

# AI 기능
anthropic==0.39.0

# 파일 처리
python-multipart==0.0.6
aiofiles==23.2.1

# CORS 지원
starlette==0.35.1

# 환경변수 관리
python-dotenv==1.0.1

# 유틸리티
jinja2==3.1.3
uuid==1.30

# 문서 형식 지원
python-docx>=1.2.0
python-pptx>=1.0.0
openpyxl>=3.1.0

# 한글(HWP) 파일 지원
olefile>=0.46
# hwp5는 PyPI에서 사용 불가, olefile로 기본 파싱 + HWPX는 zipfile로 처리
